name: Check reminders
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl "https://just-us-am.vercel.app/api/cron/check?secret=7aed3a2dfdfd7150043d1992750cbc59"
